# Plesk + Phusion Passenger
# Passenger leitet alle Anfragen automatisch an Node.js weiter.
# Diese Datei regelt NUR statische Dateien und Sicherheit.

Options -Indexes -MultiViews
DirectoryIndex disabled

# Sicherheit: PHP-Ausfuehrung in uploads/ verhindern
<FilesMatch "\.(?:php[0-9]?|phtml|phar|cgi|pl|py|sh)$">
    Require all denied
</FilesMatch>

# Sicherheits-Header
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Browsercache fuer statische Assets
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css              "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/jpeg            "access plus 6 months"
    ExpiresByType image/png             "access plus 6 months"
    ExpiresByType image/webp            "access plus 6 months"
</IfModule>
